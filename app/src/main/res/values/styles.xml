<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomBottomNavIndicator" parent="Widget.MaterialComponents.NavigationView">
        <item name="android:color">@color/orange1</item>
        <item name="android:shape">oval</item>
        <item name="android:alpha">0.5</item>
    </style>
    <style name="CustomTabText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">20sp</item> <!-- chỉnh size -->
        <item name="android:textStyle">bold</item> <!-- (tuỳ chọn) đậm -->
        <item name="android:textColor">#000000</item> <!-- (tuỳ chọn) màu chữ -->
    </style>

</resources>
